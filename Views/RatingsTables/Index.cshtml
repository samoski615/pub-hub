@model IEnumerable<PubHub.Models.RatingsTable>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BarOwnerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DrinkEnthusiastId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomerRating)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BarOwnerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DrinkEnthusiastId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerRating)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RatingsTableId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RatingsTableId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RatingsTableId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
